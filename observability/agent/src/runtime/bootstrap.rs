//! bootstrap.rs — placeholder for future agent wiring.
//!
//! Right now we keep this minimal on purpose. The actual bootstrap
//! (wiring config + event bus + runtime loops) will be defined once
//! AgentRuntime API and integration points are fully stabilised.

/// Placeholder for future agent bootstrap
#[allow(dead_code)]
pub struct AgentBootstrap;
